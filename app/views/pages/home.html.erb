<section class="resume">
  <div style="text-align: center; margin-bottom: 20px;">
    <h1>Bienvenue</h1>
      <p>Anthony Mania capture les moments authentiques, </p>
        <p>les paysages immersifs et </p>
          <p>les récits visuels pour les voyageurs, hôtels et marques inspirées.</p>
    <div>
      <%= link_to "Voir la Galerie", galerie_path %>
      <%= link_to "En savoir plus", a_propos_path %>
      <%= link_to "New York", new_york_path %>
    </div>
  </div>

  <!-- Diaporama -->
  <div class="slideshow-container">
    <%= image_tag "_MG_1955.JPG", class: "slide" %>
    <%= image_tag "_MG_2109.JPG", class: "slide" %>
    <%= image_tag "_MG_2248.JPG", class: "slide" %>
  </div>
</section>


  <section class="gallery-section">
    <div class="grid-gallery">
        <%["_MG_1955.JPG", "_MG_1956.JPG", "_MG_2109.JPG", "_MG_2110.JPG", "_MG_2270.JPG", "_MG_2321.JPG", "_MG_2247.JPG", "_MG_2248.JPG"].each do |img| %>
        <div><%= image_tag img %></div>
      <% end %>
    </div>
  </section>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    let slides = document.querySelectorAll(".slide");
    let current = 0;

    function showSlide(index) {
      slides.forEach((slide, i) => {
        slide.classList.remove("active");
      });
      slides[index].classList.add("active");
    }

    function nextSlide() {
      current = (current + 1) % slides.length;
      showSlide(current);
    }

    showSlide(current); // Affiche la première slide
    setInterval(nextSlide, 4000); // Changement toutes les 4 secondes
  });
</script>

<style>
  body {
    font-family: sans-serif;
    margin: 0;
    padding: 0;
    overflow-x: hidden;
  }
  h1 {
    font-size: 3rem;
  }
  .resume{
    padding:40px;
    bottom: 10px;
    top:10px;
    width:100%;
    height:auto;
    background-color: white;
    color:black;
  }
  .slideshow-container {
    position: relative;
    width: 100%;
    height: 100vh; /* Plein écran */
    overflow: hidden;
  }
  .slide {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;       /* Remplit sans déformer */
    object-position: center; /* Centre l'image */
    opacity: 0;
    transition: opacity 1.5s ease-in-out;
  }
  .slide.active {
    opacity: 1;
    z-index: 1;
  }
  .gallery-section {
    padding: 40px;
    background: #f9f9f9;
    text-align: center;
  }

  .gallery-section h2 {
    font-size: 2rem;
    margin-bottom: 30px;
  }

  .grid-gallery {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 16px;
    max-width: 1200px;
    margin: 0 auto;
  }

  .grid-gallery img {
    width: 100%;
    height: 180px;
    object-fit: cover;
    border-radius: 8px;
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
    transition: transform 0.2s ease;
  }
  .grid-gallery img:hover {
    transform: scale(1.03);
  }
</style>
